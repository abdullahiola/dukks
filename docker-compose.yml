services:
  dukeh-bot:
    build: .
    container_name: dukeh-bot
    restart: unless-stopped
    stop_grace_period: 10s
    env_file:
      - .env
    volumes:
      - dukeh-auth:/app/.wwebjs_auth
      - dukeh-data:/app/data

volumes:
  dukeh-auth:
  dukeh-data:
